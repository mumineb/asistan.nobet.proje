@model IEnumerable<PediatriAsistanNöbet1.Models.Model.Nobet>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Asistan.Ad)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Bolum.BolumAdi)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NobetTarihi)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Asistan.Ad)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Bolum.BolumAdi)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NobetTarihi)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.NobetID }) |
            @Html.ActionLink("Details", "Details", new { id=item.NobetID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.NobetID })
        </td>
    </tr>
}

</table>
